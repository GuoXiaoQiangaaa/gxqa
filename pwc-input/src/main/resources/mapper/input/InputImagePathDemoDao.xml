<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.pwc.modules.input.dao.InputImagePathDemoDao">

    <insert id="saveDemo">
        insert into input_image_path_demo(
        barcode,
        path,
        type
        )values
            (
            #{barCode},
            #{path},
            #{type}
            )
    </insert>

</mapper>